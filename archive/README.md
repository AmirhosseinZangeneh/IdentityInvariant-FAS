# Archive

This directory contains compatibility code for historical experiments that
should not be used as the final paper implementation.

The original exploratory workspace contained many one-off fusion, texture,
diagnostic, and plotting scripts. They are intentionally excluded from the
public research release to keep the repository reproducible and auditable.
Preserve the original private workspace separately if historical archaeology
is required.
